# Article evidence

- `guard/`: shape, scope, projection, error, and emitted-argument checks. No database.
- `http/`: generated Express routes against PostgreSQL 16. Docker required.

Each lab pins dependencies and checks output against its committed `RESULTS.txt`.
